{
  "name": "ethan-post-portfolio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "node esbuild.config.mjs",
    "dev:build": "node esbuild.config.mjs --watch",
    "dev:serve": "serve dist --listen 5173 --watch",
    "dev": "npm-run-all --parallel dev:build dev:serve"
  },
  "devDependencies": {
    "esbuild": "^0.27.2",
    "serve": "^14.2.4",
    "npm-run-all": "^4.1.5"
  }
}
