{
  "name": "ethan-post-portfolio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --watch --serve"
  },
  "devDependencies": {
    "esbuild": "^0.21.5"
  }
}
